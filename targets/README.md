# Targets

This directory is intentionally not seeded with competitor websites.

Run discovery from a source website/feed to generate:

- `targets/discovered_websites.txt`
- `targets/discovery_results.json`

Those generated files are local run output and are ignored by git.

